import numpy as np
import matplotlib.pyplot as plt


def make_polynomial(a):
    def Pm(x):


def LSA(x, y, phi):
    '''Least squares approximation'''
    n = len(x)
    a = np.zeros_like(phi)
    s = sum((sum(a * phi(x[i])) - y [i])**2 for i in range(n))

# %% Задача 5.1

a, b =
